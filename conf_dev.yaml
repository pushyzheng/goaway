server:
  port: 3000
  domain: localhost
  cookie-expired-hours: 72
  debug: true

accounts:
  admin:
    enable: true
    is-admin: true
    password: 123
  guest:
    enable: true
    password: guest

applications:
  flask:
    enable: true
    port: 5000

permissions:
  admin:
    flask:
      enable: true
      excluded-paths:
        - /admin
  guest:
    flask:
      enable: true
      excluded-paths:
        - /admin